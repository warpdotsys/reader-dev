# Production SSH remains available to key-based administrators and GitHub Actions.
# Password-based root login is disabled to prevent public brute-force traffic from
# consuming the pre-authentication connection budget used by deployments.
PasswordAuthentication no
KbdInteractiveAuthentication no
PermitRootLogin prohibit-password
MaxAuthTries 3
PerSourceMaxStartups 3
