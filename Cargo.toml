[package]
name = "reader"
version = "0.1.0"
edition = "2021"
description = "reader-dev legacy 分支全量 Rust 转录"

[dependencies]
regex = "1"
serde_json = "1"
serde = { version = "1", features = ["derive"] }
url = "2"

[profile.dev]
debug = false
