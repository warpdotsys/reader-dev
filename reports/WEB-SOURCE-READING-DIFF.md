# 固定远程书源阅读差分

验证日期：2026-09-23（Asia/Shanghai）。原始 JAR 与恢复版分别运行在随机端口和独立工作目录；同一个仅监听 `127.0.0.1` 的 Python 测试站点提供固定搜索页、书籍详情、两章目录和正文。测试使用临时账号，不接触生产数据或第三方书源。

运行命令：

```powershell
pwsh -NoProfile -File .\scripts\build.ps1
pwsh -NoProfile -File .\scripts\compare-web-source-reading.ps1
```

本轮原始 JAR SHA-256：`B26FB4769D689D98FF26408CE79A275D719F360906C84ACF52FF404E98030C8C`；最新复测恢复构建 SHA-256：`2CE4C1ABA8B40471A036C266F7E4ECFC586EB91C83878CB246194B21C25DA8EF`。结果详见 `web-source-reading-diff-latest.json`。

## 已从 JAR 验证并成功重建

八个探针中，书源保存、搜索、书籍详情、加入书架、两章目录这五项完全一致。状态码均为 200，`isSuccess=true`，`errorMsg` 为空；搜索结果、详情和目录的关键字段及正文文本由固定站点提供并经过断言。对象响应还比较了全部 JSON 字段名及默认值类别（空字符串、零、布尔值、null、非默认值）。保存书源后，搜索通过已存书源 URL 发起；加入书架后，目录及正文请求不再附带书源，验证服务器从用户数据中读回书源和书籍。

扩展字段检查时曾发现恢复版搜索结果多出内部 `_userNameSpace` 字段。已在 `SearchBook` 源码中排除序列化，并补充单测；重新构建后，搜索结果字段名与默认值类别与原始 JAR 一致。

## 已审查但不伪称一致的三项

| 探针 | 原始 JAR | 恢复版 | 判断 |
| --- | --- | --- | --- |
| 加入书架后的书架列表 | 书名、作者变成乱码，URL 与来源正确 | 书名、作者保持中文 | 保留恢复版编码修复；脚本仅在这些字段差异且其余关键字段相同时接受 |
| 第一章、第二章正文 | 正文末尾各多一个 `\n` | 正文内容相同，但无末尾多余换行 | 差异已限定为恰好一个末尾换行；暂不更改正文格式化逻辑 |

因此本轮不是“八项完全等同”：五项完全一致，三项是明确记录的差异。脚本只接受上述精确形态，其他差异会失败。末尾换行是否影响特定客户端、书签或缓存格式尚未验证，不能视为无风险。

## 尚未验证

- 本探针比较全部 JSON 字段名和默认值类别，但非默认值只逐项比较关键业务字段；动态时间戳等仍需更完整的结构契约测试。
- 本探针未覆盖真实第三方书源、JS 规则、登录态书源、翻页/发现、封面图片、正文缓存命中、SSE 搜索、书源切换和 WebDAV。独立的 SSE 单源/双源分页差分见 `reports/SSE-PAGINATION-DIFF.md`，真实书源 SSE 见 `reports/LIVE-SOURCE-READING-DIFF.md`，WebDAV 见 `reports/WEBDAV-LIFECYCLE-DIFF.md`。
- 这是本机黑盒差分，不代表线上 `read.medwarp.cn` 已部署该构建或生产数据已完成兼容验证。
